#include "main.h"

void IsRiichi(Status status, int yakunum);

void IsDoubleRiichi(Status status, int yakunum);

void IsIppatsu(Status status, int yakunum, int handTile1[]);

void IsPinfu(Status status, int yakunum, int handTile1[]);

void IsIipeikou(Status status, int yakunum, int handTile1[]);

void IsHaiteiraoyue(Status status, int yakunum, int handTile1[]);

void IsHouteiraoyui(Status status, int yakunum, int handTile1[]);

void IsRinshankaihou(Status status, int yakunum, int handTile1[]);

void IsTanyao(Status status, int yakunum, int handTile1[]);

void IsYakuhai(Status status, int yakunum, int handTile1[]);

void IsChantaiyao(Status status, int yakunum, int handTile1[]);

void IsSanshoukudoujun(Status status, int yakunum, int handTile1[]);

void IsIttsu(Status status, int yakunum, int handTile1[]);

void IsToitoi(Status status, int yakunum, int handTile1[]);

void IsSanankou(Status status, int yakunum, int handTile1[]);

void IsSankantsu(Status status, int yakunum, int handTile1[]);

void IsChiitoitsu(Status status, int yakunum, int handTile1[]);

void IsHonroutou(Status status, int yakunum, int handTile1[]);

void IsShousangan(Status status, int yakunum, int handTile1[]);

void IsHonitsu(Status status, int yakunum, int handTile1[]);

void IsJunchantaiyao(Status status, int yakunum, int handTile1[]);

void IsRyanpeikou(Status status, int yakunum, int handTile1[]);

void IsChinitsu(Status status, int yakunum, int handTile1[]);

void IsKokushimusou(Status status, int yakunum, int handTile1[]);

void IsSuuankou(Status status, int yakunum, int handTile1[]);

void IsDaisangen(Status status, int yakunum, int handTile1[]);

void IsShousuushii(Status status, int yakunum, int handTile1[]);

void IsTsuiisou(Status status, int yakunum, int handTile1[]);

void IsChinroutou(Status status, int yakunum, int handTile1[]);

void IsRyuuiisou(Status status, int yakunum, int handTile1[]);

void IsChuurenpoutou(Status status, int yakunum, int handTile1[]);

void IsSuukantsu(Status status, int yakunum, int handTile1[]);

void IsTenhou(Status status, int yakunum, int handTile1[]);

void IsChiihou(Status status, int yakunum, int handTile1[]);

void IsSuuankoutanki(Status status, int yakunum, int handTile1[]);

void IsDaisuushii(Status status, int yakunum, int handTile1[]);

void IsJunseichuurenpoutou(Status status, int yakunum, int handTile1[]);

void YakuCheck(Status status, int handTile1[], GroupInt groupInt[], int discardTile1[], int currentTile);